server=1
rpcuser=bitcoinrpc
rpcpassword=supersecurepassword
rpcallowip=0.0.0.0/0  # 允许所有来源访问（生产环境请谨慎）
rpcbind=0.0.0.0       # 让 RPC 监听所有网卡（Modal 里需要）
rpcport=8332
disablewallet=1       # 禁用内置钱包，减少资源占用（可选）
txindex=1             # 启用交易索引（可选）
